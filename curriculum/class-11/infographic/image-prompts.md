# Image Prompts — Clase 11: async/await y consumo de APIs

> Prompts para generar imágenes con AI. Cada imagen corresponde a un `<img src="...">` en `index.html`.

---

**No se requieren imágenes.** Toda la infografía es representable en HTML/CSS (código resaltado, comparativas y glosario), por lo que `index.html` no contiene ningún `<img src="...">`.
