# Image Prompts — Clase 10: Datos desde la web (fetch, Promesas y JSON)

> Prompts para generar imágenes con AI. Cada imagen corresponde a un `<img src="...">` en `index.html`.

---

**No se requieren imágenes.** Toda la infografía es representable en HTML/CSS (código resaltado y glosario), por lo que `index.html` no contiene ningún `<img src="...">`.
