---
layout: minimal
answer: incorrect
---
<!-- 
The code will produce invalid results as typeof cannot be called on typeof -->
El código producirá resultados inválidos, ya que no se puede llamar a `typeof` sobre `typeof`.
