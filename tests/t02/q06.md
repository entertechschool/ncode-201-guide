---
layout: minimal
include: all
---
<!-- ### Pick the statement that best describes the following JavaScript code
```
    let movies = ["The Matrix", "Frozen"];
    typeof(typeof(movies));
``` -->
### Elija la declaración que mejor describa el siguiente código JavaScript:
```
    let movies = ["The Matrix", "Frozen"];
    typeof(typeof(movies));
```