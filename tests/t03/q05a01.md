---
layout: minimal
answer: correct
---

<!-- The code is more readable with an appropriately named variable, rather than including an arithmetic expression inside of parentheses inside of a concatenation. -->

El código es más legible con una variable con el nombre apropiado, en lugar de incluir una expresión aritmética entre paréntesis dentro de una concatenación.