---
layout: minimal
include: all
---

### ¿Qué método de array devuelve un nuevo arreglo con los elementos que cumplan una condición?
``` javascript
const numeros = [1, 2, 3, 4, 5, 6];
const resultado = numeros._____((num) => num > 3);
// resultado debería ser [4, 5, 6]

```