---
layout: minimal
include: all
---

¿Cuál es el resultado de aplicar programación funcional en este caso?
```javascript
const números = [1, 2, 3, 4];
const resultado = números.reduce((acc, curr) => acc + curr, 0);
```