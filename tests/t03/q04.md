---
layout: minimal
include: all
---


### En el contexto de programación funcional, ¿qué hace el siguiente código?
``` javascript
const duplicar = x => x * 2;
const sumarUno = x => x + 1;
const resultado = [1, 2, 3].map(duplicar).map(sumarUno);
```