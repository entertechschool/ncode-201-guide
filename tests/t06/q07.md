---
layout: minimal
include: all
---

<!-- Which of the following statements would evaluate as truthy, given the below variable definitions?

let dog = {
 breed: 'Beagle',
 weight: 30,
 group: 'Hound'
};

let key = 'weight';
A. dog[key] === 30;

B. dog['weight'] == '30';

C. 'weight' in dog

D. dog.weight

E. dog.key === 30;

F. dog.hasOwnProperty('weight'); -->

¿Cuál de las siguientes afirmaciones se evaluaría como verdadera, dadas las siguientes definiciones de variables?

```
    let dog = {
        breed: 'Beagle',
        weight: 30,
        group: 'Hound'
    };
    let key = 'weight';
```
A. dog[key] === 30;

B. dog['weight'] == '30';

C. 'weight' in dog

D. dog.weight

E. dog.key === 30;

F. dog.hasOwnProperty('weight');