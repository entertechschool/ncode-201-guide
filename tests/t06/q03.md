---
layout: minimal
include: all
---
<!-- 
Consider the following CSS rule:


h1 { 
  border-width: 5px;
  border: dashed blue;
}

After adding the CSS properties listed above, at 100% zoom, what will be the computed width of the border around an <h1>?

Consider rendering it in your browser and checking it with the inspector. -->

Considere la siguiente regla CSS:

```
    h1 { 
        border-width: 5px;
        border: dashed blue;
    }
```
Después de agregar las propiedades CSS enumeradas anteriormente, con un zoom del 100 %, ¿cuál será el ancho calculado del borde alrededor de un `<h1>` ?

Considere la posibilidad de renderizarlo en su navegador y comprobarlo con el inspector.