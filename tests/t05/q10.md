---
layout: minimal
include: all
---

<!-- Which of the following is/are true about JavaScript objects?

A. All object properties can be accessed through "." notation

B. All object properties are automatically locally scoped but may be accessed by using ‘prototype’

C. Variables declared inside constructor functions are locally scoped and cannot be directly accessed as properties

D. Variables declared inside constructor functions are globally scoped

E. Object properties that contain boolean values may not be accessed with bracket notation and are only accessible through “.” notation -->

¿Cuál de los siguientes es/son cierto sobre los objetos JavaScript?

A. Se puede acceder a todas las propiedades del objeto a través de la notación ".".

B. Todas las propiedades del objeto tienen un alcance local, pero se puede acceder a ellas utilizando el "prototype".

C. Las variables declaradas dentro de las funciones constructoras tienen un alcance local y no se puede acceder directamente como propiedades.

D. Las variables declaradas dentro de las funciones constructoras tienen un alcance global.

E. Es posible que no se acceda a las propiedades de los objetos que contienen valores booleanos con la notación de corchetes y solo son accesibles a través de la notación "."