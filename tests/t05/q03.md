---
layout: minimal
include: all
---

<!-- What is the error in the following JavaScript code, assuming that there is a button in the corresponding HTML with an ID of "pugbomb":

let pugbombButton = document.getElementById('pugbomb');
pugbombButton.addEventListener('click', pugbombButtonHandler());

function pugbombButtonHandler() {
  alert('PUGBOMB!!!!');
} -->

¿Cuál es el error en el siguiente código JavaScript, suponiendo que haya un botón en el HTML correspondiente con un ID de "pugbomb"?

```
    let pugbombButton = document.getElementById('pugbomb');
    pugbombButton.addEventListener('click', pugbombButtonHandler());

    function pugbombButtonHandler() {
      alert('PUGBOMB!!!!');
    }
```