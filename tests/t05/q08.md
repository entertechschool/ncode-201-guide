---
layout: minimal
include: all
---
<!-- 
Consider the following JavaScript code:

function helloCodeFellows(){ 
 return 'Hello from Code Fellows!';
}
let testAlpha = helloCodeFellows();
let testBeta = helloCodeFellows;
What is the difference between variables testAlpha and testBeta? -->

Considere el siguiente código JavaScript:

```
    function helloCodeFellows(){ 
     return 'Hello from Code Fellows!';
    }
    let testAlpha = helloCodeFellows();
    let testBeta = helloCodeFellows;
```

¿Cuál es la diferencia entre las variables testAlpha y testBeta?